# Add managed Codex configuration to this file.
# The installer merges it into the user's ~/.codex/config.toml file.
model_provider = "genki"

[model_providers.genki]
  base_url = ""
  name = "Genki"
